# GitHub Pages

Use `docs/index.html` when publishing this project with GitHub Pages set to the `/docs` folder.

To update it, edit the source files or data files, then run:

```bash
node scripts/build-standalone.mjs
```
